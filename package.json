{
  "name": "vibra-ai",
  "version": "1.0.0",
  "description": "AI-powered image generation application with preset prompts and user quota management",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:netlify": "cd frontend && npm install && npx vite build --config vite.config.simple.js",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test"
  },
  "keywords": [
    "ai",
    "image-generation",
    "react",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Vibra AI Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "22.x",
    "npm": ">=8.0.0"
  },
  "render": {
    "buildCommand": "npm install && cd backend && npm install",
    "startCommand": "npm start"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
